# Files the platform-mode sweep must NOT run, and why.
#
# The sweep runs every file in tests/ except the ones listed here — EACH IN
# ITS OWN PROCESS. Measured 2026-08-03: one shared pytest session produced
# 199 failures from order-dependent global state (settings singletons,
# module-level app wiring); the same set, one process per file, produced ZERO.
# So a new
# test file is covered the moment it lands. This list may only SHRINK.
#
# Anything under RUN_MODE=agent is run by the agent-mode step instead — it is
# listed here only because the platform-mode sweep cannot run it.
#
# Files that a NAMED step in the workflow already runs are NOT listed here.
# The sweep derives that set from the workflow file itself, so it stays
# correct without a second list to keep in sync.
#
# An entry ending in `/` excuses a whole DIRECTORY.

# --- not unit tests: a live-stack package with its own runner ---
# tests/memverify/ refuses to run outside a real Postgres + pgvector with
# RUN_MODE=agent — deliberately, so its assertions can never pass vacuously
# (read its conftest). CI's sqlite/platform stack cannot create the pgvector
# `embedding` column at all. Run it with `make memory-verify`
# (backend/scripts/memory_verify.py), which provisions the DB first.
memverify/  # live Postgres+pgvector + RUN_MODE=agent; own runner, not the unit sweep

# --- run under RUN_MODE=agent (see the agent-mode step) ---
test_agent_notify_outbox.py  # agent-mode
test_autopilot_approvals.py  # agent-mode
test_autopilot_engine.py  # agent-mode
test_autopilot_tick_hygiene.py  # agent-mode
test_channel_day_consistency.py  # agent-mode
test_create_job_turn_context.py  # agent-mode
test_day_chat_unification.py  # agent-mode
test_decay.py  # agent-mode
test_job_reaper.py  # agent-mode
test_job_recovery.py  # agent-mode
test_jobs_api_no_raw_errors.py  # agent-mode
test_multichannel_default.py  # agent-mode
test_routine_dst_regression.py  # agent-mode
test_routine_one_per_kind.py  # agent-mode
test_routine_reconnect_notice.py  # agent-mode
test_routines_skill.py  # agent-mode
test_signup_hardening.py  # agent-mode
test_subagent_context_isolation.py  # agent-mode: _build_system_prompt SELECTs `identities` (AGENT_ONLY); 8/8 green under agent, 3 of 4 platform failures were this lane and not staleness
test_subagent_dispatcher.py  # agent-mode
test_toup_media_and_audio_first.py  # agent-mode: media_playlists is AGENT_ONLY (26/26 green under agent, 7 red under platform)
test_subagent_orchestrator.py  # agent-mode
test_subagent_credit_budget.py  # agent-mode
test_subagent_orphan_sweep.py  # agent-mode
test_trigger_health_and_reverify.py  # agent-mode
test_trigger_schema.py  # agent-mode
test_triggers_api.py  # agent-mode

# --- QUARANTINED: red on 2026-08-03, none of them ever run by CI ---
test_agent_defer_boot_init.py  # asserts something the code no longer does
test_agent_runner_subagent_params.py  # fails for a reason not yet triaged
test_api.py  # needs a real database; sqlite has no such table/column
test_auth_timezone_from_coords.py  # needs a real database; sqlite has no such table/column
test_autopilot_handoff.py  # needs a real database; sqlite has no such table/column
test_backfill_day_chats.py  # exceeds a 240s per-file budget
test_connector_dispatcher.py  # needs a real database; sqlite has no such table/column
test_connector_mcp.py  # asserts something the code no longer does
test_connector_providers_t3_t4.py  # needs a real database; sqlite has no such table/column
test_connector_schema.py  # asserts something the code no longer does
test_consolidation.py  # references code that no longer exists
test_conversation_resolver.py  # fails for a reason not yet triaged
test_cron_service_deprecation.py  # fails for a reason not yet triaged
test_cron_tool_removed.py  # references code that no longer exists
test_day_chat_resolver.py  # exceeds a 240s per-file budget
test_day_chats_api.py  # exceeds a 240s per-file budget
test_email_briefing_handler.py  # fails for a reason not yet triaged
test_friendly_error_provider_mapping.py  # asserts something the code no longer does
test_gap_features.py  # fails for a reason not yet triaged
test_grandfather_byo_llm_mode.py  # asserts something the code no longer does
test_layer6.py  # fails for a reason not yet triaged
test_mcp_auth.py  # fails for a reason not yet triaged
test_models_endpoint.py  # asserts something the code no longer does
test_notify_coverage.py  # needs a real database; sqlite has no such table/column
test_oauth_flow.py  # needs a real database; sqlite has no such table/column

# --- red on its own, and my file scan never even saw it ---
# My sweep globbed tests/test_*.py and missed subdirectories entirely.
agent/test_system_prompt_assembly.py  # 2 tests fail standalone; never scanned, never run

# --- TRIAGED 2026-08-04: red under the exact CI env, cause known ---
# Every line below carries a DIAGNOSIS, not a shrug. The previous round wrote
# "fails for a reason not yet triaged" nine times; each of those is now either
# fixed or replaced by what is actually wrong and what to do about it.
#
# needs BOTH halves of the AGENT_ONLY / PLATFORM_ONLY split at once, so no
# single RUN_MODE can build its schema — the file has to be re-partitioned or
# the split has to grow a "both" lane:
test_prewarm_phase1.py  # MODE: soul_configs (platform) + agent-side save path
test_reminder_countdown.py  # MODE: walks the reminder lifecycle across both partitions
test_sessions_day_dedup.py  # MODE: conversations+day_chats vs user_sessions
test_trigger_production_hardening.py  # MODE: triggers (agent) + connector_identities (platform); passes 42/42 unpartitioned
#
# the behaviour deliberately changed and the assertion never followed. NOT a
# production defect in any of these — each was checked against current source:
test_routine_next_run_at_and_refresh_durability.py  # STALE: source-grep; runner.py:637 does call _sync_next_run
test_routine_outcome_state_machine.py  # STALE: asserts RoutineRun; f06c260d made build_jobs the sole per-fire row
test_routine_runner.py  # STALE: same cutover — the two failures are the only RoutineRun tests
test_routine_three_production_bugs.py  # STALE: two source-text greps over code that legitimately changed
test_routines_phase3_verification.py  # STALE: same jobs cutover, from the data side
test_triggers_inbound.py  # STALE: SELECTs TriggerEvent, removed by the unified-jobs cutover
#
# the test's own scaffolding is wrong; neither the assertion nor production is:
test_token_budget.py  # INFRA: stub lacks _meter_web_tool + pins a tail the INJ-2 fence changed (the real bug it found shipped in #431)
